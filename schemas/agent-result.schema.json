{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "message": {
      "type": "string",
      "description": "The concise final response to send to the user."
    },
    "attachments": {
      "type": "array",
      "description": "Absolute local paths of files that should be sent to the user.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "path": { "type": "string" },
          "caption": { "type": ["string", "null"] }
        },
        "required": ["path", "caption"]
      }
    },
    "session_disposition": {
      "type": "string",
      "enum": ["continue", "handoff"],
      "description": "Whether the native provider session must remain available for the immediate next request."
    },
    "memory_refs": {
      "type": "array",
      "description": "Changed core/profile.md or core/preferences.md path, or an empty list when no direct user memory was needed.",
      "items": { "type": "string" }
    }
  },
  "required": ["message", "attachments", "session_disposition", "memory_refs"]
}
